{
    "name": "rpa-extension",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json && node build.mjs",
        "test": "pnpm build && node tests/workspace_state.test.mjs"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.279",
        "typescript": "^5.9.2"
    }
}
